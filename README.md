# wavreader
This is a small command line utility which reads a .wav file and prints it out as a series of nibbles. I wrote it for the purpose of generating waveforms for chiptune music production.

Options:
``-i`` provide input filepath.
``-s`` specify amount of samples to print out.

